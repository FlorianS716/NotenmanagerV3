<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane styleClass="root" stylesheets="@notenmanagerTheme.css"
           xmlns="http://javafx.com/javafx/17.0.12"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="at.wifi.notenmanager.controller.GradingCompController">

    <VBox alignment="CENTER" spacing="20" styleClass="card" maxWidth="950">

        <!-- Titel -->
        <Label styleClass="heading" text="Bewertungskomponenten für dieses Fach" />

        <!-- Eingabebereich -->
        <TitledPane expanded="true" styleClass="titled-pane" text="Neue Komponente hinzufügen / bearbeiten">
            <content>
                <GridPane hgap="12" vgap="10">
                    <padding><Insets top="10" right="10" bottom="10" left="10"/></padding>
                    <columnConstraints>
                        <ColumnConstraints halignment="RIGHT" minWidth="140" />
                        <ColumnConstraints hgrow="ALWAYS" />
                    </columnConstraints>
                    <Label text="Name:" styleClass="subheading" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
                    <TextField fx:id="nameField" promptText="z. B. Lesen" styleClass="text-field"
                               GridPane.columnIndex="1" GridPane.rowIndex="0"/>
                </GridPane>
            </content>
        </TitledPane>

        <!-- Buttons & Status -->
        <HBox alignment="CENTER_LEFT" spacing="12">
            <Label fx:id="statusLabel" styleClass="subheading"/>
            <Button fx:id="addButton" onAction="#onAdd" text="Hinzufügen" styleClass="button login-button"/>
            <Button fx:id="deleteButton" onAction="#onDelete" text="Löschen" styleClass="button logout-button"/>
            <Button fx:id="saveButton" onAction="#onSave" text="Speichern" styleClass="button" disable="true"/>
        </HBox>

        <!-- Tabelle -->
        <TableView fx:id="componentTable" prefHeight="340" styleClass="table-view">
            <columns>
                <TableColumn fx:id="idColumn" prefWidth="60" text="ID"/>
                <TableColumn fx:id="nameColumn" prefWidth="240" text="Komponente"/>
                <TableColumn fx:id="weightColumn" prefWidth="520" text="Gewichtung (%)"/>
            </columns>
            <placeholder>
                <Label text="Noch keine Bewertungskomponenten vorhanden."/>
            </placeholder>
        </TableView>

        <!-- Gesamtsumme -->
        <HBox alignment="CENTER_RIGHT">
            <Label fx:id="totalLabel" styleClass="subheading"/>
        </HBox>

        <!-- Info -->
        <TitledPane expanded="false" text="🛈 Was macht dieser Bereich?" maxWidth="900">
            <VBox >
            <Label style="-fx-font-weight: bold;"
                   text="In diesem Bereich können Sie Ihre Unterrichtsfächer individuell gewichten." wrapText="true" />
            <Label text="• Legen Sie Ihre Komponenten an (z.B. Diktate, Referate, usw) ." wrapText="true" />
            <Label text="• Stellen Sie nun die prozentuale Gewichtung der Gesamtnote dieses Faches ein.
            (Gewichtung muss 100% ergeben)" wrapText="true" />
            <Label text="• Speichern Sie die neue Gewichtung. Kann bei Bedarf geändert werden." wrapText="true" />
            </VBox>
        </TitledPane>

    </VBox>
</StackPane>
