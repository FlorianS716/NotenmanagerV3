<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane styleClass="root"
           stylesheets="@notenmanagerTheme.css"
           xmlns="http://javafx.com/javafx/17.0.12"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="at.wifi.notenmanager.controller.GradesController">

    <!-- zentrierter Card-Container -->
    <VBox alignment="CENTER" spacing="24" styleClass="card" maxWidth="950">

        <!-- Überschrift -->
        <Label text="Notenverwaltung" styleClass="heading"/>

        <!-- Filterzeile -->
        <GridPane hgap="12" vgap="10">
            <columnConstraints>
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="ALWAYS" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="ALWAYS" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>

            <Label text="Schüler:" styleClass="subheading" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
            <ComboBox fx:id="studentComboBox" promptText="Schüler wählen" styleClass="combo-box"
                      prefWidth="200" GridPane.columnIndex="1" GridPane.rowIndex="0"/>

            <Label text="Fach:" styleClass="subheading" GridPane.columnIndex="2" GridPane.rowIndex="0"/>
            <ComboBox fx:id="subjectComboBox" promptText="Fach wählen" styleClass="combo-box"
                      prefWidth="200" GridPane.columnIndex="3" GridPane.rowIndex="0"/>

            <Label text="Komponente:" styleClass="subheading" GridPane.columnIndex="4" GridPane.rowIndex="0"/>
            <ComboBox fx:id="componentComboBox" promptText="z.B. Diktat" styleClass="combo-box"
                      prefWidth="180" GridPane.columnIndex="5" GridPane.rowIndex="0"/>
        </GridPane>

        <!-- Noteingabe -->
        <GridPane hgap="12" vgap="10">
            <columnConstraints>
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="NEVER" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="NEVER" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="NEVER" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>

            <Label text="Note (1–5):" styleClass="subheading" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
            <Spinner fx:id="noteSpinner" prefWidth="100" GridPane.columnIndex="1" GridPane.rowIndex="0"/>

            <Label text="Datum:" styleClass="subheading" GridPane.columnIndex="2" GridPane.rowIndex="0"/>
            <DatePicker fx:id="gradeDatePicker" GridPane.columnIndex="3" GridPane.rowIndex="0"/>

            <Button text="Speichern" onAction="#handleSave" styleClass="button login-button"
                    GridPane.columnIndex="4" GridPane.rowIndex="0"/>
            <Button text="Löschen" onAction="#handleDelete" styleClass="button logout-button"
                    GridPane.columnIndex="5" GridPane.rowIndex="0"/>
        </GridPane>

        <!-- Tabelle -->
        <Label text="Vergebene Noten" styleClass="subheading"/>
        <TableView fx:id="gradeTable" prefHeight="300" styleClass="table-view">
            <columns>
                <TableColumn fx:id="componentCol" text="Komponente" prefWidth="180"/>
                <TableColumn fx:id="dateCol" text="Datum" prefWidth="160"/>
                <TableColumn fx:id="noteCol" text="Note" prefWidth="535"/>
            </columns>
            <placeholder>
                <Label text="Noch keine Noten eingetragen."/>
            </placeholder>
        </TableView>

        <!-- Info -->
        <TitledPane text="🛈 Was macht dieser Bereich?" expanded="false" maxWidth="900">
            <VBox spacing="6" styleClass="card">
                <padding><Insets top="12" bottom="12" left="12" right="12"/></padding>
                <Label text="In diesem Bereich können Sie Ihren Schülern pro Fach und Komponente Noten zuweisen."
                       wrapText="true" style="-fx-font-weight: bold;" />
                <Label text="• Wählen Sie den Schüler aus." wrapText="true"/>
                <Label text="• Wählen Sie das Fach aus." wrapText="true"/>
                <Label text="• Wählen Sie die zu bewertende Komponente." wrapText="true"/>
                <Label text="• Geben Sie die Note ein." wrapText="true"/>
                <Label text="• Wählen Sie das Datum." wrapText="true"/>
                <Label text="• Speichern Sie den neuen Eintrag oder löschen Sie bestehende." wrapText="true"/>
            </VBox>

        </TitledPane>

        <!-- Status -->
        <HBox alignment="CENTER_LEFT" spacing="12">
            <Label fx:id="statusLabel" styleClass="subheading"/>
            <Pane HBox.hgrow="ALWAYS"/>
            <Label fx:id="avgLabel" styleClass="subheading"/>
        </HBox>

    </VBox>
</StackPane>
