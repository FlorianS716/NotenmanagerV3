<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane styleClass="root" stylesheets="@notenmanagerTheme.css" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="at.wifi.notenmanager.controller.GradesController">

    <!-- zentrierter Card-Container -->
    <VBox alignment="CENTER" maxWidth="950" spacing="24" styleClass="card">

        <!-- Überschrift -->
        <Label styleClass="heading" text="Notenverwaltung" />

        <!-- Filterzeile -->
        <GridPane hgap="12" vgap="10">
            <columnConstraints>
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="ALWAYS" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="ALWAYS" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>

            <Label styleClass="subheading" text="Schüler:" GridPane.columnIndex="0" GridPane.rowIndex="0" />
            <ComboBox fx:id="studentComboBox" prefWidth="200" promptText="Schüler wählen" styleClass="combo-box" GridPane.columnIndex="1" GridPane.rowIndex="0" />

            <Label styleClass="subheading" text="Fach:" GridPane.columnIndex="2" GridPane.rowIndex="0" />
            <ComboBox fx:id="subjectComboBox" prefWidth="200" promptText="Fach wählen" styleClass="combo-box" GridPane.columnIndex="3" GridPane.rowIndex="0" />

            <Label styleClass="subheading" text="Komponente:" GridPane.columnIndex="4" GridPane.rowIndex="0" />
            <ComboBox fx:id="componentComboBox" prefWidth="180" promptText="z.B. Mitarbeit" styleClass="combo-box" GridPane.columnIndex="5" GridPane.rowIndex="0" />
         <rowConstraints>
            <RowConstraints />
         </rowConstraints>
        </GridPane>

        <!-- Noteingabe -->
        <GridPane hgap="12" vgap="10">
            <columnConstraints>
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="NEVER" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="NEVER" />
                <ColumnConstraints halignment="RIGHT" minWidth="100" />
                <ColumnConstraints hgrow="NEVER" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>

            <Label styleClass="subheading" text="Note (1–5):" GridPane.columnIndex="0" GridPane.rowIndex="0" />
            <Spinner fx:id="noteSpinner" prefWidth="100" GridPane.columnIndex="1" GridPane.rowIndex="0" />

            <Label styleClass="subheading" text="Datum:" GridPane.columnIndex="2" GridPane.rowIndex="0" />
            <DatePicker fx:id="gradeDatePicker" GridPane.columnIndex="3" GridPane.rowIndex="0" />

            <Button onAction="#handleSave" styleClass="button login-button" text="Speichern" GridPane.columnIndex="4" GridPane.rowIndex="0" />
            <Button onAction="#handleDelete" styleClass="button logout-button" text="Löschen" GridPane.columnIndex="5" GridPane.rowIndex="0" />
         <rowConstraints>
            <RowConstraints />
         </rowConstraints>
        </GridPane>

        <!-- Tabelle -->
        <Label styleClass="subheading" text="Vergebene Noten" />
        <TableView fx:id="gradeTable" prefHeight="300" styleClass="table-view">
            <columns>
                <TableColumn fx:id="componentCol" prefWidth="180" text="Komponente" />
                <TableColumn fx:id="dateCol" prefWidth="160" text="Datum" />
                <TableColumn fx:id="noteCol" prefWidth="535" text="Note" />
            </columns>
            <placeholder>
                <Label text="Noch keine Noten eingetragen." />
            </placeholder>
        </TableView>

        <!-- Info -->
        <TitledPane expanded="false" maxWidth="900" text="🛈 Was macht dieser Bereich?">
            <VBox spacing="6" styleClass="card">
                <padding><Insets bottom="12" left="12" right="12" top="12" /></padding>
                <Label style="-fx-font-weight: bold;" text="In diesem Bereich können Sie Ihren Schülern pro Fach und Komponente Noten zuweisen." wrapText="true" />
                <Label text="• Wählen Sie den Schüler aus." wrapText="true" />
                <Label text="• Wählen Sie das Fach aus." wrapText="true" />
                <Label text="• Wählen Sie die zu bewertende Komponente." wrapText="true" />
                <Label text="• Geben Sie die Note ein." wrapText="true" />
                <Label text="• Wählen Sie das Datum." wrapText="true" />
                <Label text="• Speichern Sie den neuen Eintrag oder löschen Sie bestehende." wrapText="true" />
            </VBox>

        </TitledPane>

        <!-- Status -->
        <HBox alignment="CENTER_LEFT" spacing="12">
            <Label fx:id="statusLabel" styleClass="subheading" />
            <Pane HBox.hgrow="ALWAYS" />
            <Label fx:id="avgLabel" styleClass="subheading" />
        </HBox>

    </VBox>
</StackPane>
