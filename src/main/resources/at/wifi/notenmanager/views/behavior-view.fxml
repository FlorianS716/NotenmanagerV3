<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane styleClass="root" stylesheets="@notenmanagerTheme.css"
           xmlns="http://javafx.com/javafx/17.0.12"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="at.wifi.notenmanager.controller.BehaviorController">

    <!-- Zentrierter Container -->
    <VBox alignment="CENTER" spacing="30" maxWidth="950" styleClass="card">

        <!-- Formularblock -->
        <GridPane hgap="12" vgap="12">
            <columnConstraints>
                <ColumnConstraints halignment="RIGHT" minWidth="140" />
                <ColumnConstraints hgrow="ALWAYS" />
            </columnConstraints>
            <rowConstraints>
                <RowConstraints />
                <RowConstraints />
                <RowConstraints />
                <RowConstraints />
                <RowConstraints />
            </rowConstraints>

            <Label text="Schüler:" GridPane.columnIndex="0" GridPane.rowIndex="0" styleClass="subheading" />
            <ComboBox fx:id="studentComboBox" promptText="Schüler auswählen" onAction="#onFilterChanged"
                      GridPane.columnIndex="1" GridPane.rowIndex="0" styleClass="combo-box" />

            <Label text="Fach:" GridPane.columnIndex="0" GridPane.rowIndex="1" styleClass="subheading" />
            <ComboBox fx:id="subjectComboBox" promptText="Fach auswählen" onAction="#onFilterChanged"
                      GridPane.columnIndex="1" GridPane.rowIndex="1" styleClass="combo-box" />

            <Label text="Datum:" GridPane.columnIndex="0" GridPane.rowIndex="2" styleClass="subheading" />
            <DatePicker fx:id="datePicker" GridPane.columnIndex="1" GridPane.rowIndex="2" styleClass="table-view" />

            <Label text="Mitarbeitsnote (1–5):" GridPane.columnIndex="0" GridPane.rowIndex="3" styleClass="subheading" />
            <ComboBox fx:id="ratingComboBox" promptText="Note wählen"
                      GridPane.columnIndex="1" GridPane.rowIndex="3" styleClass="combo-box" />

            <Label text="Kommentar:" GridPane.columnIndex="0" GridPane.rowIndex="4" styleClass="subheading" />
            <TextArea fx:id="commentArea" promptText="Optionaler Kommentar"
                      prefRowCount="3" GridPane.columnIndex="1" GridPane.rowIndex="4" />
        </GridPane>

        <!-- Aktionen -->
        <HBox alignment="CENTER_RIGHT" spacing="10">
            <Button text="Speichern" onAction="#onSave" styleClass="button login-button" />
            <Button text="Löschen" onAction="#onDelete" styleClass="button logout-button" />
            <Button text="Aktualisieren" onAction="#onEdit" styleClass="button" />
        </HBox>

        <!-- Tabelle -->
        <TableView fx:id="behaviorTable" prefHeight="220" maxWidth="900" styleClass="table-view">
            <columns>
                <TableColumn fx:id="dateColumn" prefWidth="120" text="Datum" />
                <TableColumn fx:id="ratingColumn" prefWidth="80" text="Note" />
                <TableColumn fx:id="commentColumn" prefWidth="724" text="Kommentar" />
            </columns>
            <placeholder>
                <Label text="Keine Einträge vorhanden." />
            </placeholder>
        </TableView>

        <!-- Info -->
        <TitledPane expanded="false" text="🛈 Was macht dieser Bereich?" maxWidth="900">
            <VBox spacing="6" prefWidth="500">
                <Label text="In diesem Reiter ist es Ihnen möglich, das Verhalten eines Schülers direkt zu dokumentieren."
                       wrapText="true" style="-fx-font-weight: bold;" />
                <Label text="• Wählen Sie zuerst den betroffenen Schüler aus." wrapText="true"/>
                <Label text="• Danach wählen Sie das Fach, indem Sie das Verhalten eines Schüler vermerken wollen." wrapText="true"/>
                <Label text="• Das Datum für den Eintrag." wrapText="true"/>
                <Label text="• Eine Benotung, um für Ihre pädagogische Einschätzung einen Richtwert zu haben." wrapText="true"/>
                <Label text="• Optional: einen Kommentar, um den Eintrag mit Details zu ergänzen." wrapText="true"/>
                <Label text="• WICHTIG: Aktualisierung ist nur mit erneuter Eingabe der Note möglich." wrapText="true"/>

            </VBox>
        </TitledPane>

    </VBox>
</StackPane>
