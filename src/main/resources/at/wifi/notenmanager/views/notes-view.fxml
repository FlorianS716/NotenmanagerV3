<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<StackPane styleClass="root" stylesheets="@notenmanagerTheme.css"
           xmlns="http://javafx.com/javafx/17.0.12"
           xmlns:fx="http://javafx.com/fxml/1"
           fx:controller="at.wifi.notenmanager.controller.NotesController">

    <VBox alignment="CENTER" spacing="24" styleClass="card" maxWidth="800">

        <!-- Titel -->
        <Label styleClass="heading" text="Meine Notizen"/>

        <!-- Notizbereich -->
        <Label styleClass="subheading" text="Notiz bearbeiten oder hinzufügen:"/>
        <TextArea fx:id="noteTextArea" prefRowCount="4" wrapText="true" styleClass="text-field"/>

        <!-- Buttons -->
        <HBox alignment="CENTER_RIGHT" spacing="12">
            <Pane HBox.hgrow="ALWAYS"/>
            <Button fx:id="saveButton" onAction="#handleSaveNote" text="Speichern" styleClass="button login-button"/>
            <Button fx:id="deleteButton" onAction="#handleDeleteNote" text="Löschen" styleClass="button logout-button"/>
        </HBox>

        <!-- Notizenliste -->
        <Label styleClass="subheading" text="Gespeicherte Notizen"/>
        <ListView fx:id="notesListView" prefHeight="300" styleClass="list-view"/>

        <!-- Info -->
        <TitledPane expanded="false" text="🛈 Was macht dieser Bereich?" maxWidth="700">
            <VBox spacing="6" styleClass="card">
                <padding>
                    <Insets top="12" right="12" bottom="12" left="12"/>
                </padding>
                <Label text="In diesem Bereich können Sie beliebige Notizen erfassen..." wrapText="true"/>
                <Label text="Die Daten werden lokal gespeichert und sind nur für Sie sichtbar." wrapText="true"/>
            </VBox>
        </TitledPane>

    </VBox>
</StackPane>
